export interface Service {
  title: string;
  description: string;
  icon?: string;
}

export interface CoreValue {
  title: string;
  description: string;
}

export interface CompanyInfo {
  name: string;
  founded: number;
  tagline: string;
  vision: string;
  mission: string;
}